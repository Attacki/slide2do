# notes for `packages/agent-skills/`

> 最后更新: 2026-07-21 — 新建本笔记；当前为空桩

wuzi-agent 预制能力包（agent skills）。预期承载可复用的角色工作流编排，便于跨角色共享行为模式。

## 状态

- （空 / 待实现）当前仅有 `package.json`，无具体 skill 实现
- 后续新增 skill 时按 `packages/agent-skills/<skill-name>/` 组织，并在本笔记登记
